/**
 * The product's one display name. The manifest (name + action tooltip) and every
 * in-product surface read it from here so the casing can never drift — the npm
 * package id in package.json stays lowercase and is a separate identifier.
 */
export const BRAND_NAME = "HeaderShim";
